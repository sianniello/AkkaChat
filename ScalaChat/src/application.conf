//#chatserver
chatserver {
  include "common"

  akka {
    remote.netty.tcp {
      #hostname = "127.0.0.1"
      #port = 1099
    }
  }
}

//#client
chatclient {
  include "common"
  akka {
  	remote.netty.tcp.hostname="127.0.0.1"
    remote.netty.tcp.port = 2553
  }
}